// parses field data and returns a list format that can be used to render the
// section list

import { useProjectInfo } from "../app/stores/project_info";

export function parseFields() {

  // get fields for project

}